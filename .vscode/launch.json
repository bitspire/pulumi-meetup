{
  "version": "0.2.0",
  "configurations": [

    {
      "type": "node-terminal",
      "name": "Run Script: dev in nix",
      "request": "launch",
      "command": "nix-shell --run 'yarn run dev'",
      "cwd": "${workspaceFolder}"
    }
  ]
}